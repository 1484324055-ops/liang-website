# 个人网站性能与质量报告

## 当前状态

- 更新日期：2026-07-15
- 框架：Astro 6.4.8
- 构建命令：`npm run build`
- 构建结果：通过
- 构建产物：20 个文件，约 442.9 KB
- 外部字体请求：0
- 本地生产预览 LCP：约 1.50 秒
- 本地生产预览 CLS：0

以上性能数据来自本机生产预览，用于版本间对比，不等同于所有用户网络环境下的线上成绩。

## 已完成优化

### 1. 视觉系统

- 保留首页打字机动画和黑金视觉。
- 使用全页液态金属 WebGL 场景连接九个章节。
- WhyMe、ROI、案例、证书、服务、流程、FAQ 和联系区具有统一的滚动反馈。
- 桌面端右侧章节轨支持点击跳转、键盘操作和当前章节高亮。

### 2. WebGL 性能

- 液态金属场景由 Three.js 改为原生 WebGL。
- 主视觉 JavaScript 从约 501 KB 降至约 12.4 KB。
- 移除未使用的 `three`、`@types/three` 和 `gsap` 依赖。
- 支持移动端降低像素密度、页面隐藏时暂停、减少动画偏好和 WebGL 失败回退。

### 3. 字体与资源

- 移除 Google Fonts，改用系统字体栈。
- 删除 `public` 中与 `src/assets` 重复的证书、头像和二维码文件。
- 页面图片由 Astro 构建为 WebP 等优化格式。
- 微信二维码构建后约 43.5 KB。

### 4. 页面交互

- ROI 计算器支持轻量尝试、成长业务、高投入团队和自定义参数。
- ROI 结果提供月度节省、年度节省、工时和工作日解读。
- 六个案例支持右侧详情抽屉。
- 案例抽屉支持遮罩关闭、Esc、焦点循环、焦点恢复和手机全屏显示。
- 证书支持点击查看大图。

### 5. 可访问性与兼容性

- 支持 `prefers-reduced-motion`。
- 主要交互具备键盘焦点样式和 ARIA 状态。
- 桌面端与 390 × 844 手机视口均无横向溢出。
- WebGL 不可用时自动显示 CSS 备用背景。

## 主要构建文件

| 文件 | 未压缩大小 |
|---|---:|
| 全站 CSS | 约 66.8 KB |
| 液态金属与章节控制脚本 | 约 12.4 KB |
| 案例详情抽屉脚本 | 约 5.9 KB |
| 微信二维码 WebP | 约 43.5 KB |
| 头像 WebP | 约 1.8 KB |

## 资源位置

- 头像：`src/assets/images/avatar.jpg`
- 微信二维码：`src/assets/images/wechat-qr.png`
- 证书：`src/assets/certs/`
- 分享图：`public/images/og-default.png`
- 页面源码：`src/`

## 发布前检查

```powershell
cd "D:\Projects\个人网站"
npm install
npm run build
npm run preview -- --host 127.0.0.1 --port 4323
```

发布前应检查：

1. 首页打字机和液态金属画面正常。
2. 右侧章节轨可以跳转。
3. ROI 预设和滑块计算正确。
4. 六个案例抽屉可以打开、关闭并跳转联系区。
5. 证书大图、二维码和外部平台链接正常。
6. 桌面端和手机端无横向溢出。
